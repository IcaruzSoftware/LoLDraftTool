import type { Position } from '../data/types';

/** Short uppercase role labels shown in slots and filters. */
export const ROLE_LABEL: Record<Position, string> = {
  top: 'TOP',
  jungle: 'JGL',
  mid: 'MID',
  bot: 'BOT',
  support: 'SUP',
};
